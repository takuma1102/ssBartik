% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline.R
\name{ssbartik}
\alias{ssbartik}
\title{One-call shift-share analysis}
\usage{
ssbartik(
  data,
  shares,
  shocks,
  y = "y",
  x = "x",
  location = "location",
  sector = "sector",
  time = NULL,
  controls = NULL,
  weights = NULL,
  cluster = NULL,
  exogenous = c("shift", "share"),
  covariates = NULL,
  pre_y = NULL,
  top = 5,
  level = 0.95
)
}
\arguments{
\item{data}{A unit-level `data.frame`: one row per location (or
location-period). Must contain `y`, `x`, `location`, and any `controls`,
`weights`, `cluster` columns referenced below.}

\item{shares}{A long `data.frame` of exposure shares with columns
`location`, `sector`, `share` (and `time` for panels).}

\item{shocks}{A `data.frame` of shocks with columns `sector`, `shock`
(and `time` for panels).}

\item{y, x}{Column names (strings) of the outcome and endogenous treatment.}

\item{location, sector}{Column names of the unit and sector identifiers.}

\item{time}{Optional column name of a period identifier (present in `data`,
`shares` and `shocks`) for panel designs.}

\item{controls}{Optional character vector of numeric control columns in
`data`.}

\item{weights}{Optional column name of regression weights in `data`.}

\item{cluster}{Optional column name of a clustering variable in `data`.}

\item{exogenous}{Which identification route to emphasise downstream:
`"shift"` (shocks) or `"share"` (shares). `"shock"`/`"shares"` are accepted
aliases.}

\item{covariates, pre_y, top, level}{Passed to [ssb_pipeline()].}
}
\value{
An `ssb_result` object.
}
\description{
Convenience wrapper that builds an [ssb_design()] from raw pieces and runs
[ssb_pipeline()] --- the "give me everything" entry point. Specify the
identification route with `exogenous` and the rest flows through to
diagnostics and plots.
}
\examples{
sim <- ssb_simulate(seed = 1)
res <- ssbartik(sim$data, sim$shares, sim$shocks,
                controls = "w1", weights = "pop", exogenous = "share")
res
\dontrun{
autoplot(res)                       # headline Rotemberg figure
autoplot(res$estimate)              # SE comparison
}
}
