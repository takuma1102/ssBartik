% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/table_image.R
\name{ssb_table_image}
\alias{ssb_table_image}
\alias{plot.ssb_estimate}
\alias{plot.ssb_weight_summary}
\alias{plot.ssb_overid}
\alias{plot.ssb_loo}
\alias{plot.ssb_drop_top}
\title{Render a result table as an image (PNG or PDF)}
\usage{
\method{plot}{ssb_estimate}(x, file = NULL, width = NULL, height = NULL, res = 200, digits = 3, ...)

\method{plot}{ssb_weight_summary}(x, file = NULL, width = NULL, height = NULL, res = 200, digits = 3, ...)

\method{plot}{ssb_overid}(x, file = NULL, width = NULL, height = NULL, res = 200, digits = 3, ...)

\method{plot}{ssb_loo}(x, file = NULL, width = NULL, height = NULL, res = 200, digits = 3, ...)

\method{plot}{ssb_drop_top}(x, file = NULL, width = NULL, height = NULL, res = 200, digits = 3, ...)
}
\arguments{
\item{x}{A result object: an [ssb_estimate()] (also [ssb_placebo()]),
[ssb_weight_summary()], [ssb_overid()], [ssb_loo()], or [ssb_drop_top()].}

\item{file}{Output path (`.png` or `.pdf`); `NULL` draws on the current device.}

\item{width, height}{Image size in inches (auto-sized when `NULL`).}

\item{res}{PNG resolution in dpi.}

\item{digits}{Number of decimal places.}

\item{...}{Unused.}
}
\value{
The `file` path, invisibly.
}
\description{
`plot()` methods that draw the same booktabs-style table [format()] prints,
as a standalone image --- the sibling of [plot.ssb_rotemberg()]. Pass `file=`
to write a `.png` (default) or `.pdf`; without `file` the table is drawn on
the current graphics device. For LaTeX/Markdown source instead of an image,
use `format(x, "latex")` / `format(x, "markdown")`.
}
