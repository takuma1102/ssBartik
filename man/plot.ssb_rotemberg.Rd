% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rotemberg_table.R
\name{plot.ssb_rotemberg}
\alias{plot.ssb_rotemberg}
\title{Render the Rotemberg-weight table as a compact booktabs figure}
\usage{
\method{plot}{ssb_rotemberg}(
  x,
  file = NULL,
  width = NULL,
  height = NULL,
  res = 200,
  n = 6,
  digits = 3,
  note = .ssb_rot_note(),
  ...
)
}
\arguments{
\item{x}{An [ssb_rotemberg()] object.}

\item{file}{Optional output path; the format is taken from the extension
(`.png` or `.pdf`).}

\item{width, height}{Figure size in inches; defaults adapt to the content.}

\item{res}{Resolution in PPI for the `.png` device (ignored for `.pdf`).}

\item{n}{Number of top-weight shocks to include.}

\item{digits}{Decimal places for the estimates and weights.}

\item{note}{Footnote shown left-aligned below the table (an italic "Note:"
label is prepended). Defaults to a short definition of the columns; pass
`NULL` to omit it, or a custom string.}

\item{...}{Unused.}
}
\value{
The object, invisibly (called for its side effect).
}
\description{
Draws the top-weight-shock table (see [format.ssb_rotemberg()] for the
columns) as a paper-style image with normal single-line row spacing. Writes
to the active device, or to `file` when supplied (`.png` default, `.pdf` for
vector output). For LaTeX/Markdown source use [format()] instead; for the
bubble scatter use [ssb_plot_rotemberg()].
}
